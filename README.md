# VectorLab 網站專案

## 使用方式

1. 解壓縮 ZIP。
2. 使用 Visual Studio Code 開啟 `VectorLab-VSCode` 資料夾。
3. 直接開啟 `index.html`，或使用 VS Code 的 Live Server 擴充功能預覽。

## 檔案結構

- `index.html`：網站頁面
- `styles.css`：網站樣式與響應式版面
- `script.js`：行動選單、輪播、訂閱提示與導覽效果

網站字型使用 Google Fonts，預覽時需要網路連線。
